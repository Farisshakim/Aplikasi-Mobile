// File: src/config.js

const IP_ADDRESS = "192.168.100.182";
const PORT = "8000";

// API URL (Untuk ambil data teks)
export const API_BASE_URL = `http://${IP_ADDRESS}:${PORT}/`;

// IMAGE URL (Untuk ambil gambar)
export const IMAGE_URL = `http://${IP_ADDRESS}:${PORT}/uploads/`;
